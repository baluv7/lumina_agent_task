How to Run agent_graph.py (LangGraph + Mistral)

1. ✅ Place `agent_graph.py` in your working folder.

2. ✅ Create and activate a Python virtual environment:
   > python -m venv langgraph_env
   > langgraph_env\Scripts\activate  (on Windows)

3. ✅ Install the required packages:
   > pip install langgraph langchain langchain-ollama

4. ✅ Install and run Ollama:
   - Download Ollama from: https://ollama.com/download
   - Then in terminal run:
     > ollama pull mistral
     > ollama run mistral

   (Keep Ollama running while using the script.)

5. ✅ Run the Python script:
   > python agent_graph.py

6. 🧪 You will see the test results printed:
   - Summary
   - Math
   - Translator
   - Time
   - Fallback

7. 📌 Output Example:
   ✅ Final Output: <response>
